// Create an object with a property of first_name and last_name. Add your
// first and last names. Then console log out the full name using the
// function that accesses the object data in the console.

var name = {
    first_name : "Sanjiwani",
    last_name : "Jha"
};

console.log(name.first_name+name.last_name);
console.log(name.first_name+" "+name.last_name);

